---
import FragmentStream from '../components/FragmentStream.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import pages from '../data/pages.json';
import siteData from '../data/site.json';
import { getFragments } from '../lib/fragments.js';

const fragments = getFragments({ mode: 'science' });
---

<BaseLayout
  title={`${pages.science.title} | ${siteData.siteDefaults.title}`}
  description={pages.science.description}
>
  <FragmentStream
    fragments={fragments}
    title={pages.science.title}
    intro={pages.science.intro}
  />
</BaseLayout>
